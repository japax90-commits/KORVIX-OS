"use client";
import {useState} from "react";import{createClient}from"@/lib/supabase/client";import{Plus,X}from"lucide-react";
export function CreateCashMovementButton({userId}:{userId:string}){const[open,setOpen]=useState(false),[busy,setBusy]=useState(false),[error,setError]=useState("");async function submit(fd:FormData){setBusy(true);setError("");const amount=Number(fd.get("amount")||0),description=String(fd.get("description")||"").trim();if(amount<=0||!description){setError("Informe valor e descrição.");setBusy(false);return}const{error:e}=await createClient().from("cash_movements").insert({category:String(fd.get("category")||"outros"),direction:String(fd.get("direction")||"entrada"),amount,description,movement_date:String(fd.get("movement_date")||new Date().toISOString().slice(0,10)),created_by:userId});if(e){setError(e.message);setBusy(false);return}window.location.reload()}return<><button onClick={()=>setOpen(true)} className="flex items-center gap-2 rounded-lg bg-korvix-900 px-4 py-2.5 text-sm font-medium text-white"><Plus size={16}/> Lançar movimento</button>{open&&<div className="fixed inset-0 z-50 flex items-center justify-center bg-black/40 p-4"><form action={submit} className="w-full max-w-lg space-y-4 rounded-2xl bg-white p-6 shadow-2xl"><div className="flex justify-between"><div><h3 className="text-lg font-semibold">Movimento de caixa</h3><p className="text-sm text-ink-500">Entrada ou saída real.</p></div><button type="button" onClick={()=>setOpen(false)}><X/></button></div><div className="grid grid-cols-2 gap-3"><select name="direction" className="rounded-lg border p-2.5"><option value="entrada">Entrada</option><option value="saida">Saída</option></select><select name="category" className="rounded-lg border p-2.5"><option>reserva</option><option>marketing</option><option>comercial</option><option>tecnologia</option><option>contratacao</option><option>expansao</option><option>outros</option></select></div><input name="amount" type="number" min="0.01" step="0.01" required placeholder="Valor" className="w-full rounded-lg border p-2.5"/><input name="description" required placeholder="Descrição" className="w-full rounded-lg border p-2.5"/><input name="movement_date" type="date" defaultValue={new Date().toISOString().slice(0,10)} className="w-full rounded-lg border p-2.5"/>{error&&<p className="rounded bg-red-50 p-3 text-sm text-red-700">{error}</p>}<div className="flex justify-end gap-2"><button type="button" onClick={()=>setOpen(false)} className="rounded-lg border px-4 py-2">Cancelar</button><button disabled={busy} className="rounded-lg bg-korvix-900 px-4 py-2 text-white">{busy?"Salvando...":"Salvar"}</button></div></form></div>}</>}
