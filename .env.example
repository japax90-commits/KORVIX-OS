# Variáveis de ambiente previstas na Especificação Técnica Master V2.0 (seção 39).
# Nenhuma é necessária para rodar este protótipo visual — ele usa apenas dados
# mockados em /lib/mock-data.ts. Ficam documentadas aqui para a próxima etapa,
# quando o projeto passar a usar Prisma/PostgreSQL e NextAuth de verdade.

DATABASE_URL=
DIRECT_URL=
NEXTAUTH_SECRET=
NEXTAUTH_URL=http://localhost:3000
EMAIL_SERVER=
EMAIL_FROM=
CRON_SECRET=
NODE_ENV=development
SENTRY_DSN=
